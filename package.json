{
  "type": "module",
  "scripts": {
    "postinstall": "cd examples && npm i",
    "docs:dev": "npm run vituum:build && vitepress dev docs",
    "docs:build": "npm run vituum:build && vitepress build docs",
    "docs:serve": "vitepress serve docs",
    "vituum:dev": "cd examples && npx vite",
    "vituum:build": "cd examples && npx vite build",
    "vituum:preview": "cd examples && npx vite preview"
  },
  "dependencies": {
    "vitepress": "^1.2.2",
    "vue": "^3.4.27",
    "winduum": "^1.2.0-next.8"
  }
}
